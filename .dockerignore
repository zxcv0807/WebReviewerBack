# Git 저장소
.git
.gitignore

# Python 캐시
__pycache__/
*.pyc
services/__pycache__/

# 가상환경 (Docker에서 새로 생성)
venv/

# 환경변수 (docker-compose에서 주입)
.env

# 로그 파일 (Docker에서 새로 생성)  
logs/

# 업로드 폴더 (Supabase Storage 사용)
uploads/

# 문서 파일 (런타임에 불필요)
README.md
CLAUDE.md

# Docker 설정 파일들
Dockerfile
docker-compose.yml
.dockerignore

# 배포 스크립트
deploy_v2.sh